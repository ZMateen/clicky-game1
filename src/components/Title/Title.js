import React from "react";
import "./Title.css";

const Title = props => (
    <div className = "titlebox">
        <h1>American McGee's Alice Clicky Game!</h1>
        <p>Click on an image to earn you some points! Just don't click on any picture more than once!</p>
    </div>
);

export default Title;